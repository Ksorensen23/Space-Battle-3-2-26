import time
import sys

alive == True
  while True:     
    def typewriter(str, sec):
      for char in str
        sys.stdout.write(char)
        sys.stdout.flush()
        time.sleep(sec)

    def textCleanUp(inp):
      if inp.isdigit():
        retVal = "error no answer selected"
      else:
        retVal = inp.upper()
      return retVal
